import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ReiniciarContrasenaPage } from './reiniciar-contrasena.page';

describe('ReiniciarContrasenaPage', () => {
  let component: ReiniciarContrasenaPage;
  let fixture: ComponentFixture<ReiniciarContrasenaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ReiniciarContrasenaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
